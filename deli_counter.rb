katz_deli = []

def line(array)
  if array.length == 0
    puts "The line is currently empty."
  elsif array.length > 0
  array.each do |name|
    return "The line is currently 1. name."
  end
end
end


def take_a_number(array, name)
end

def now_serving(array)
end
